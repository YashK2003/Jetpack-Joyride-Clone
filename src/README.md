 `To compile the game open the folder and run the following commands`

`mkdir build`

`cd build`

`cmake ..`

`make`

`./app`

`press the spacebar to move up`

`collect the coins to increase the score`

`Enjoy the Game !!`

